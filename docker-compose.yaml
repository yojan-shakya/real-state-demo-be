services:
  pg:
    image: postgres
    env_file:
      - ./.env.docker
    ports:
      - 8008:8008

  app:
    build: .
    env_file:
      - './.env.docker'
    ports:
      - '3000:3000'
    depends_on:
      - pg
